=== AST ===
type Punto(x, y) { ... }
let p = new Punto(NumberLiteral: 1.000000, NumberLiteral: 2.000000) in print(Variable(p).z)

=== TREE ===
Program
  declarations
    TypeDecl(name=Punto)
      ctorParams
        Param(name=x)
        Param(name=y)
      members
        TypeMemberAttribute(name=x)
          VariableReference(name=x)
        TypeMemberAttribute(name=y)
          VariableReference(name=y)
  globalExpr
    LetIn
      bindings
        VariableBinding(name=p)
          NewExpr(type=Punto)
            Number(value=1)
            Number(value=2)
      body
        Print
          MemberAccess(name=z)
            VariableReference(name=p)
