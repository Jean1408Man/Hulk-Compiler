type Counter(max: Number) {
    i = 0;
    limit = max;

    next(): Boolean => {
        self.i := self.i + 1;
        self.i <= self.limit;
    };

    current(): Number => self.i;
}

let c = new Counter(3) in
    for (x in c) print(x);
