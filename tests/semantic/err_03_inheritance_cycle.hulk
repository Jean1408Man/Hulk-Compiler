// Corte 7 — Test err_03: ciclo de herencia
// Esperado: error semántico "ciclo de herencia detectado en tipo A"

type A(x) inherits B(x) {
    val = x;
}

type B(x) inherits A(x) {
    val = x;
}

print("no debería llegar aquí");
