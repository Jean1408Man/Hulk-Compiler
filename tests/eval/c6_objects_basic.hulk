type Point(x, y) {
    x = x;
    y = y;
    getX() => self.x;
    getY() => self.y;
    distance() => sqrt(self.x * self.x + self.y * self.y);
}

let p = new Point(3, 4) in {
    print(p.getX());
    print(p.getY());
    print(p.distance());
};
